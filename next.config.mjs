/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'logo.clearbit.com',
          'via.placeholder.com',
          'fakeimg.pl'
          // Add any other domains you need
        ],
      },
};

export default nextConfig;
